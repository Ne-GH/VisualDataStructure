/*
#include "MainWindow.h"
#include <QApplication>

#include "MSTL/BinaryTree.hpp"
*/

import Config;

int main(int argc, char *argv[]) {
    Config::Config file("./config.txt");
    /*
    QApplication a(argc, argv);
    MainWindow main_window;
    main_window.show();
    return a.exec();
*/
}